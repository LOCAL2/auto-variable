# 🚀 วิธีตั้งค่า Short URL System

## ขั้นตอนที่ 1: สร้าง Gist Database

1. ไปที่ https://gist.github.com/
2. สร้าง Gist ใหม่:
   - Filename: `shorturl-db.json`
   - Content: `{}`
   - เลือก "Create secret gist" หรือ "Create public gist" (แนะนำ secret)

3. คัดลอก Gist ID จาก URL
   - ตัวอย่าง URL: `https://gist.github.com/username/abc123def456`
   - Gist ID คือ: `abc123def456`

## ขั้นตอนที่ 2: ตั้งค่า Environment Variable

เปิดไฟล์ `.env.local` และเพิ่ม Gist ID:

```
VITE_GITHUB_TOKEN=your_github_token_here
VITE_SHORT_URL_GIST_ID=abc123def456
```

## ขั้นตอนที่ 3: Restart Dev Server

```bash
npm run dev
```

## 🎉 เสร็จแล้ว!

ตอนนี้เมื่อสร้างลิงก์ใหม่ จะได้ URL สั้นแบบนี้:

- **ก่อน:** `https://auto-variable.vercel.app/77ed478d408df917411f0091ed5a762e`
- **หลัง:** `https://auto-variable.vercel.app/s/aBc12` ✨

## ข้อดี

✅ ฟรี 100% (ใช้ GitHub Gist)
✅ ไม่จำกัดจำนวน
✅ URL สั้นมาก (5-6 ตัวอักษร)
✅ ควบคุมเองได้ทั้งหมด
✅ ไม่ต้องพึ่งบริการภายนอก

## หมายเหตุ

- Short URL จะถูกสร้างอัตโนมัติเมื่อสร้างลิงก์ใหม่
- ถ้า Gist ID เดิมถูกสร้าง short URL แล้ว จะใช้ code เดิม
- QR Code จะใช้ short URL โดยอัตโนมัติ
